{
  "name": "Ditnails",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tw-build": "tailwindcss -i src/css/style.css -o src/public/css/style.css",
    "tw-watch": "npx tailwindcss -i src/css/style.css -o src/public/css/style.css --watch",
    "develop": "start npx tailwindcss -i src/css/style.css -o src/public/css/style.css --watch & nodemon init",
    "build": "tailwindcss -i src/css/style.css -o src/public/css/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.4",
    "tailwindcss": "^3.4.7"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  }
}
